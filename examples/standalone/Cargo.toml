[package]
name = "concordance-standalone"
version = "0.1.0"
edition = "2021"

[dependencies]
concordance-core = { path = "../../core" }
serde_json = "1.0"
hex = { workspace = true }
ed25519-dalek = { workspace = true, features = ["serde"] }
